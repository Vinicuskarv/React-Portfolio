import NavBar from '../../components/navbar';
import ToolsContainer from './ContainerInfoTools';

function Tools() {
  return (
    <div>
        <NavBar/>
        <ToolsContainer/>
    </div>
  );
}

export default Tools;